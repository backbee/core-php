{% set transparencyClass = '' %}
{% set picto = this.getObject().getParamValue('picto') %}

{% if transparency.value %}
    {% set transparencyClass = 'transparency' %}
{% endif %}

<div
    {% block data_attributes %}{% endblock %}
    {{this.bbcontent(null, {
        class: style.value ~ ' ' ~ transparencyClass ~ ' content paragraph' ~ (bg_color.value == '' ? '' : ' has-bg-color ' ~ bg_color.value)~ (picto != '' ? ' with-picto d-flex' : '') ~ (picto_direction != '' ? ' picto-' ~  picto_direction.value : '')
    })|raw}}
>
    {% if picto != '' %}
        <img src="{{this.getCdnImageUrl(picto)|raw}}"alt="" class="image-picto picto-{{picto_size.value}}">
    {% endif %}

    <div class="rte-text" data-cloud-rte="true">
        {{value|raw}}
    </div>
</div>
